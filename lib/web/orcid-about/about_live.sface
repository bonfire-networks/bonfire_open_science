{#if Settings.get([Bonfire.OpenScience, :affiliations_widget], false, @__context__) == true}
  <div class="w-full">
    {#for %{edge: %{object: %{id: _id, media_type: "orcid", path: path, metadata: metadata}}} <- @aliases}
      <Bonfire.OpenScience.OpenAlex.AffiliationsLive id="affiliation" user={@user} aliases={@aliases} />
    {/for}
  </div>
{/if}
